# TODO - Receipt PDF UI (Tips/Logo/Colors)

- [ ] Update `tipsgalwar_admin/src/pages/Payments.jsx` client-side `generateReceiptPDF()` to add better UI: colored header band, logo, better typography/spacing.
- [ ] Ensure `jsPDF` / `jspdf-autotable` usage works (no breaking runtime errors).
- [ ] Wire the new PDF generation into `downloadReceipt()` if currently it downloads server PDF. (Keep server endpoint unchanged if required.)
- [ ] Re-run lint/build to ensure no syntax issues.

